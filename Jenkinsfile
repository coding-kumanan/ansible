pipeline {
    agent any
    }
    stages{
        stage('Performing a dry run') {  // This stage should only run when you raise a PULL Request.
            steps {
                sh '''
                    env 
                
                '''
            }
        }
    }
}
